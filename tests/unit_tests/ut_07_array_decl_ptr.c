// rm129

// Array declarations combined with pointer declarators.
// Tests both "array of pointers" (int *arr[10]) and "pointer to array" (int (*pa)[10]) patterns.
// Coverage: pointer-array combinations and nested declarators.
